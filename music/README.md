Drop your licensed audio files here:

- `Beautiful In White.mp3` for English mode
- `Ngôi nhà hạnh phúc.mp3` for Vietnamese mode

You can also set custom URLs in `.env`:

- `VITE_MUSIC_EN_URL`
- `VITE_MUSIC_VI_URL`
